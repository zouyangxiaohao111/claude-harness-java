package com.nexusai.application.agent.workflow;

/**
 * {@code skipped} 变体 · CC original: {@code {kind:'skipped'}} (types.ts:52)。
 * pendingAction skip 路径产出，无字段。
 */
public record AgentRunResultSkipped() implements AgentRunResult {
}
